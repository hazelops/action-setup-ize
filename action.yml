# JavaScript action
name: action-setup-ize
description: Setup ize tool
branding:
  icon: 'terminal'
  color: 'blue'

inputs:
  version:
    description: Version of the ize to use
    required: false
    default: 0.3.0

runs:
  using: 'node20'
  main: './dist/index.js'
