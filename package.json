{
  "name": "ize",
  "version": "1.0.0",
  "description": "For more information please visit ize.sh",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "ncc build index.js --minify",
    "watch": "ncc build index.js --watch --minify"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@actions/core": "^1.2.6",
    "@actions/tool-cache": "^1.1.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1"
  }
}
